name: Build Docker images for AequilibraE CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # - name: Log in to registry
      #   run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # - name: Build and Push
      #   shell: powershell
      #   run: |
      #     docker build -t ${{ secrets.CONTAINER_REGISTRY_USERNAME }}/windows-ci:latest -f Dockerfile-windows-ci ci/aequilibrae/
      #     docker push ${{ secrets.CONTAINER_REGISTRY_USERNAME }}/windows-ci:latest

      - name: Build Windows CI
        shell: powershell
        run: |
          dir
          docker build -t windows-ci:latest -f Dockerfile-windows-ci ci\aequilibrae\
        
  build-ubuntu:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # - name: Log in to registry
      #   run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # - name: Build and Push
      #   shell: powershell
      #   run: |
      #     docker build -t ${{ secrets.CONTAINER_REGISTRY_USERNAME }}/windows-ci:latest -f Dockerfile-windows-ci ci/aequilibrae/
      #     docker push ${{ secrets.CONTAINER_REGISTRY_USERNAME }}/windows-ci:latest

      - name: Build Ubuntu CI
        run: docker build -t ubuntu-ci:latest -f Dockerfile-ubuntu-ci ci/aequilibrae/
